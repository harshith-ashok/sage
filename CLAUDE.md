# SAGE — Sovereign Agentic (Grounded) Engine — Master Plan

Self-hosted, air-gapped AI workbench for confidential industrial/government work. **No component makes a network call after initial model download — ever.** If you're about to add a dependency or API call that reaches the internet at runtime, stop and flag it instead of adding it silently.

## Session discipline — read this before doing anything

This file drives many separate Claude Code sessions across a hackathon build. To avoid burning context/credit:

- **Work on exactly one unchecked phase per session.** Find the first `- [ ]` phase below, do only that phase's tasks, check them off, then **stop and summarize** — don't cascade into the next phase automatically. The user will say "continue" or name the next phase explicitly.
- **Don't view whole files/repos speculatively.** When porting from `insurance_claim_agent/` or `hi/`, grep or view the specific file/function you're adapting — not the full repo tree. use the CLAUDE.md (and/or) README.md files inside each repo as source of information
- **Prefer small, targeted edits over rewrites.** Use `str_replace` for a few changed lines, not a full-file rewrite, unless the phase genuinely requires a new file.
- **Don't re-read this file's already-completed phases in depth** — the checkboxes are the state; trust them.
- **Don't regenerate `PROJECT_PLAN.md` or `SAGE_Overview.md`** — they're reference docs with the full rationale for each phase. Consult them only if a phase's one-line summary here isn't enough to proceed.
- **No speculative scaffolding.** Build what the current phase asks for, not what a future phase might need.

## Project layout

```
insurance_claim_agent/   # reference repo — hybrid RAG, eval harness. PORT FROM, don't build in.
hi/                      # reference repo — model router, MCP tools, sandboxed shell. PORT FROM.
backend/                 # uv-based Python project — BUILD TARGET
frontend/                # Vue 3 + Vite skeleton — BUILD TARGET
PROJECT_PLAN.md          # detailed rationale per phase — consult, don't duplicate
SAGE_Overview.md         # feature/product doc — consult for language+TTS design details
```

## Stack

LangGraph orchestrator · Ollama/vLLM model serving · Qdrant hybrid RAG (dense + BM25 + RRF + reranker) · Docker sandbox (no network) · `python-docx`/`pptx`/`openpyxl` · Vue 3 + Tailwind + Vite frontend, Pinia, SSE streaming · IndicTrans2 + Indic Parler-TTS for language layer · scikit-learn for the ML tool.

## Acceptance checklist — the build isn't done until every box is true, demonstrated live, not just implemented

- [ ] Model auto-selection across ≥2 task types, routed via config, not if/else
- [ ] New model addable by editing `models.yaml` only — zero orchestrator code touched
- [ ] Agentic doc task: scanned report → real `.docx` approval note, end to end
- [ ] Coding task: written, run in sandbox, verified against expected output, retries on failure
- [ ] Multimodal task on a second artifact type (not the same file as the doc task)
- [ ] Knowledge base grounding: turn it off, confirm the answer changes
- [ ] Network monitor visibly shows zero external calls for the full duration of every demo above

---

## Phases

### Phase 0 — Environment setup `[CORE]`

- [x] `uv sync` works in `backend/`; `npm install && npm run dev` works in `frontend/`
- [x] Pull 2–3 sized-for-venue-GPU models via Ollama (coding, reasoning, vision) — before the demo window, not during
      — reasoning: `gpt-oss:120b-cloud` (cloud) / `qwen3:8b` (local); coding: `kimi-k2.7-code:cloud` (cloud) / `qwen2.5-coder:7b` (local); vision: `gemma4:31b-cloud` (cloud only, **no local vision model pulled yet** — flag for Phase 1/8)
- [x] Qdrant running locally; Docker sandbox has no network access, confirmed with a trivial round trip
      — Qdrant container `sage-qdrant` up on :6333/:6334 (persisted via `sage_qdrant_storage` volume); `docker run --network none` confirmed blocked vs. default network confirmed reachable

### Phase 1 — Backend: model router `[CORE]`

- [x] `backend/config/models.yaml`: `task_type → {model_id, endpoint, context_window}`
      — `backend/config/models.yaml`; task types `reasoning`/`coding`/`vision`/`embedding`, each with an `active` key + a `candidates` map (`model_id`, `location: local|cloud`, `context_window`). `location` flags which candidates break zero-egress (see file header) — every task type keeps a `local` candidate alongside the `-cloud` one used for dev now.
- [x] Port routing logic from `hi/hi/router.py` + `hi/hi/models.py` (persona → task_type)
      — `backend/app/router.py` (persona/tier → task_type), `backend/app/ollama_client.py` (`is_pulled`/`ensure_pulled`, generalized from tier names), `backend/app/model_warmup.py` (`warm_up`/`invoke_with_retry`, ported verbatim)
- [x] LangGraph orchestrator: plan → act → verify loop
      — `backend/app/orchestrator.py`, generic skeleton; concrete tools/prompts per task type land in Phases 4/5/7
- [x] Prove: add an unused model via config only, route to it live, zero code touched
      — `backend/app/main.py` (`GET /models`, `POST /models/{task_type}/active`, `POST /route/{task_type}/invoke`); live-tested: invoked `reasoning` on cloud `gpt-oss:120b-cloud`, switched `active` to local `qwen3:8b` via the API (persisted to `models.yaml` on disk), re-invoked and got a correct response from the local model — no orchestrator/router code touched, then switched back to the cloud default
      — **Note (Phase 6):** `POST /route/{task_type}/invoke` described above as synchronous was later converted to the async task_id+SSE pattern (`orchestrator.run_streaming`, `GET /route/{task_type}/invoke/{task_id}/stream`) for Console's live trace panel — see Phase 6

### Phase 2 — Frontend: UI shell `[CORE]`

- [x] Audit `frontend/package.json`; add Pinia, Vue Router, virtualization lib if missing
      — Vue Router + Tailwind v4 already present; added `pinia` (installed, wired in `main.ts`, no stores yet — first real one lands with Phase 6's screen wiring) and `@tanstack/vue-virtual` for `VirtualDataTable`
- [x] Tailwind tokens: neutral palette + one accent, deliberate type pairing — no generic AI-template look
      — `frontend/src/style.css`, Tailwind v4 `@theme` tokens (dark neutral palette + blue accent, ported from `insurance_claim_agent/frontend`'s token system) + Space Grotesk/Inter/JetBrains Mono type pairing. Fonts are **self-hosted** (`frontend/src/assets/fonts/*.woff2`, fetched once from Google Fonts and bundled) rather than loaded from `fonts.googleapis.com` at runtime like the reference repo does — that CDN link would violate SAGE's zero-egress guarantee, so it wasn't ported as-is
- [x] App shell: sidebar (Console, Knowledge Base, Deliverables, Model Registry, Network Monitor, Settings)
      — `frontend/src/components/layout/AppShell.vue` + `frontend/src/router/index.ts`; each section is a placeholder view (`frontend/src/views/*.vue`) noting which later phase wires it, so Phase 2 stays scoped to shell + navigation
- [x] API client wrapping fetch + EventSource, mirroring `insurance_claim_agent/frontend`'s SSE pattern
      — `frontend/src/lib/api.ts`: `apiGet`/`apiPost` (fetch) + `streamSSE` (generalized version of `useQueryDemo.ts`'s named-event handling, reusable across Phase 4/5/7's task-streaming endpoints instead of being query-specific)
- [x] Base components: Button, Card, StatusPill, VirtualDataTable, StreamingText
      — `frontend/src/components/base/`; `vue-tsc -b` passes clean and `npm run dev` serves all six routes (200s, no transform errors) — **not** verified in an actual browser (no browser/screenshot tool available in this environment), so visual rendering is unconfirmed beyond compile + serve correctness

### Phase 3 — Backend: local knowledge base + confidence flagging `[CORE]`

- [x] Port `ingestion/`, `retrieval/`, `inference/` from `insurance_claim_agent/`, repoint at sample SOP corpus
      — `backend/app/knowledge/` (`parsing.py`/`pipeline.py`/`dependencies.py`/`search.py`/`guardrail.py`/`llm.py`/`query.py`), logic ported near-verbatim except: dense embeddings go through `app/knowledge/embeddings.py` → the Phase 1 router's `embedding` task type (not a hardcoded `sentence-transformers` model) and `llm.py` generates via `app/router.py`'s `reasoning` task type (not a hardcoded model name) — both config-driven per Phase 1's contract. Reranker (`cross-encoder/ms-marco-MiniLM-L-6-v2`) stays a directly-held `sentence-transformers` `CrossEncoder`, same as the reference repo (one-time HF download, fully local after). Corpus: 4 synthetic industrial SOP PDFs (valve inspection, confined space entry, pressure vessel inspection, lockout/tagout — `backend/data/sop_docs/`, generated since no real corpus was available), ingested via `scripts/ingest_knowledge.py` into Qdrant collection `sage_sop_docs` (16 chunks)
- [x] Small golden set + `eval/` harness reuse for a real recall/precision number
      — `backend/eval/` (`golden_set.jsonl`: 15 hand-written questions — 10 answerable, 2 ambiguous/cross-doc, 3 unanswerable — ground truth ×(source, page) pulled from a live Qdrant scroll; `dataset.py`/`metrics.py`/`conftest.py`/`test_retrieval.py` adapted from the reference repo's eval suite, scoped to just recall@k/precision@k/MRR per this phase's ask, not the full abstention/consistency/RAGAS suite). Live result: `uv run pytest eval/ -v -s` → **mean recall@4 = 1.000, mean precision@4 = 0.521** over the 12 questions with expected citations
- [x] Extend `guardrail.py` citation check into confidence flagging on low-confidence extractions
      — `app/knowledge/guardrail.py`'s `flag_confidence()`: aggregates `verify_citations` output + the top retrieval score into one `high`/`low`/`unverifiable` signal (`ConfidenceFlag`, `app/knowledge/schemas.py`) surfaced on every `run_query()` result as `confidence`, independent of the (still disabled-by-default) hard abstention gate. Live-tested both ends: a grounded question → `"level": "high"`; an out-of-corpus question → correctly abstained **and** flagged `"level": "low"` (top retrieval score −10.2)
- [x] `GET /knowledge/search` endpoint
      — `backend/app/main.py`; live-tested end to end against the ingested SOP corpus (see above)

### Phase 4 — Backend: agentic document task + reviewer sub-agent `[CORE]`

- [x] Vision/OCR pipeline (via Ollama, config-driven "vision" task type) reads scanned inspection report
      — `app/tasks/document.py`'s `ocr_node`, using `get_chat_model("vision")` (Phase 1 router) — not hardcoded to a specific vision model, so it follows whichever candidate is active (currently `gemma4:31b-cloud`, with local `qwen2.5vl:7b` as the fallback pulled in Phase 0). Sample input: a synthetic "scanned" Form MI-014 valve inspection report (`data/sample_inputs/inspection-report.png`, generated since no real scan was available) with a deliberately flawed technician recommendation to test the reviewer node
- [x] Reasoning model drafts note, grounded via Phase 3
      — `draft_node`: grounds via `app/knowledge/query.py`'s new `retrieve_context()`/`format_context()` (retrieval-only reuse of the Phase 3 pipeline, no wasted generation call), drafts via `get_chat_model("reasoning")`, every requirement cited `(Page X, Section Y)`
- [x] Reviewer/critic LangGraph node checks draft against SOPs before finalizing
      — `review_node` + conditional edge back to `draft` on rejection (bounded, `MAX_ATTEMPTS = 3`), same plan→act→verify shape as Phase 1's orchestrator. Live-tested end to end: the sample report's technician recommendation ("standard 48-hour reporting") conflicted with SOP-VLV-100 Clause 6.1 (severe atmospheric leak requires an _immediate_ shutdown request) — the draft correctly overrode the technician's recommendation and cited the real SOP requirement, and the reviewer approved it on the first pass
- [x] `python-docx` output; `POST /tasks/document` streams progress
      — `finalize_node` writes a real `.docx` via `python-docx`, downloaded and verified as valid OOXML with the full approval note; `POST /tasks/document` (multipart image upload) starts the task and returns a `task_id`, `GET /tasks/document/{task_id}/stream` streams `ocr_done`/`draft_done`/`review_done`/`done` as SSE (`app/task_stream.py`, a generic background-task+SSE bridge — POST can't be consumed by the browser's GET-only `EventSource`, so progress streaming is split across these two endpoints; Phase 5/7 reuse the same bridge). `GET /deliverables/{filename}` serves the finished file

### Phase 5 — Backend: sandboxed coding task + verified calculations `[CORE]`

- [x] Adapt `hi/hi/agents/term/danger.py` + `shell_pane.py` into sandboxed code-exec tool
      — `danger.py` ported verbatim to `app/tasks/danger.py` as a pre-flight refuse-if-destructive gate. `shell_pane.py` turned out **not** to fit "sandboxed": it runs commands in a tmux pane visible to and shared with the user's own shell (the opposite of isolation) and needs an interactive tmux session a headless backend doesn't have — nothing there was reusable, so `app/tasks/sandbox.py` builds real sandboxing fresh: `docker run --network none` (a throwaway temp dir as the only mount, deleted after every run), matching the Stack's actual "Docker sandbox (no network)" line. Live-tested: normal code runs correctly, `danger`-flagged code is refused pre-flight, and a genuine network call inside the sandbox fails with DNS resolution error — the isolation is real, not just configured
- [x] Execute → verify actual output vs expected → report; test one deliberate failure + retry
      — `app/tasks/code.py`: generate (`get_chat_model("coding")`) → execute (sandbox) → verify (exact stdout match) → retry with real stdout/stderr feedback, bounded `MAX_ATTEMPTS = 3`, same graph shape as Phase 1/4. Live-tested both outcomes: a simple task passed on attempt 1; a deliberately fiddly exact-format task genuinely failed twice (trailing comma, then an off-by-one) before exhausting retries and reporting `passed: false` with the real diagnostic — proving the loop retries on **actual execution feedback**, not just prompt tweaking. (Caught and fixed a real bug in the process: LangGraph reports a node's `{}` return as `None` in the updates stream, which crashed the naive `dict(update)` driver — fixed in both this and Phase 4's document task driver.)
- [x] Calculation tool: sympy/numpy, units tracked, steps shown — never freeform LLM arithmetic
      — `app/tasks/calculate.py`: sympy parses/evaluates/unit-converts (`sympy.physics.units`), numpy (via `sympy.lambdify`) vectorizes when a variable is given as an array — the caller supplies the expression and values, sympy/numpy always do the arithmetic. Live-tested: scalar calc, unit conversion (N/m² → Pa), and array vectorization all correct, each returning its `steps`
- [x] `POST /tasks/code`, `POST /tasks/calculate`
      — `backend/app/main.py`; `/tasks/code` follows the same background-task+SSE pattern as Phase 4's `/tasks/document` (`app/task_stream.py`, reused as designed); `/tasks/calculate` is a plain synchronous POST (no LLM in the loop, nothing to stream). **Flagged, not silently worked around:** the requested coding default `kimi-k2.7-code:cloud` returned `403 this model requires a subscription` from Ollama Cloud — an account-level limit, not a bug. Testing above used the local `qwen2.5-coder:7b` fallback (switched live via the Phase 1 registry API), which is what `coding`'s `active` candidate is left pointing at now; switch it back once the Ollama Cloud subscription covers that model
      — **Two fixes found live-testing a real statistics-heavy prompt (full writeup under Phase 6):** the sandbox image was bare `python:3.12-slim` with no packages and no network ever, so any numpy/scipy-based generated code failed every retry — replaced with a custom `docker/sandbox/Dockerfile` → `sage-sandbox:latest` (numpy+scipy pre-installed at build time, the deliberate one-time-download exception; built once via `docker build -t sage-sandbox:latest backend/docker/sandbox/`). Also, `expected_output` is now optional in `run_python`/`run_code_task`/`CodeTaskState`: open-ended analytical tasks have no single correct stdout string to match, so when it's omitted, `verify_node` instead checks the script actually ran (`exit_code == 0`) and printed something, rather than failing correct code against a guessed expected string.

### Phase 6 — Frontend: wire core screens `[CORE]`

- [x] Console: task input + live trace panel (model/tools/retries) via SSE
      — `frontend/src/views/ConsoleView.vue`: a task-kind switcher (Reasoning/Coding/Vision generic route, Document, Sandboxed code, Calculate) driving all of Phases 1/4/5's endpoints, with a shared trace panel rendering each SSE event live. Required a backend change: `POST /route/{task_type}/invoke` (Phase 1) was synchronous — converted to the same background-task+SSE pattern as `/tasks/document`/`/tasks/code` (`orchestrator.run_streaming()`, new `GET /route/{task_type}/invoke/{task_id}/stream`) so Console has one consistent live-trace contract across every task type. Live-tested end to end: `plan_done → act_done → verify_done → done`, correct payloads throughout
      — **Redesigned same-day per explicit request:** the task-kind picker above was replaced with a single free-form chat input (no task type, optional image attach) — a Claude Code/Codex-style experience. New `backend/app/agent.py`: a LangGraph `create_react_agent` (bind-tools ReAct loop) bound to the `reasoning` model, wrapping Phases 3/4/5 as tools (`search_knowledge_base`, `read_uploaded_image`, `run_sandboxed_code`, `calculate`, `draft_docx_approval_note`) — the model decides which tool(s) a request needs, generalizing Phase 7's "orchestrator picks tool from natural language" to everything, not just the ML tool. New `POST /agent/chat` (multipart: prompt + optional image) + `GET /agent/chat/{task_id}/stream` (`tool_call`/`tool_result`/`message`/`done` events); `ConsoleView.vue` rewritten as a chat transcript. The old task-kind-picker UI and its dedicated streaming endpoints (`/route/{task_type}/invoke`, `/tasks/document`, `/tasks/code`, `/tasks/calculate`) were left in place at the time (used as tools/for direct API use) — the picker itself is gone from the UI. **Update, same phase, later in the build:** most of those endpoints turned out to never actually get called again once the picker was gone — see the "simplify the architecture" entry near the bottom of this section, which removed all of them except `/tasks/document`.
      — **Caught a real correctness regression while testing:** without Phase 4's forced reviewer step, the free-form agent's first pass on the escalation test case reproduced the technician's error (approved the 48h-only recommendation, missing SOP-VLV-100's immediate-shutdown clause) — root cause was `search_knowledge_base` returning a pre-synthesized answer from only `top_k=4`, and the escalation clause's chunk ranked 5th. Fixed by having the tool return raw retrieved excerpts (`retrieve_context`/`format_context`, `top_k=6`) for the agent to reason over directly instead of a narrower pre-digested answer, plus a system-prompt instruction to search again specifically for escalation/reporting requirements before drafting safety-critical content. Re-tested the identical scenario live: correctly cited Clause 6.1 and the immediate shutdown requirement this time — verified by reading the actual generated `.docx` content, not just the chat response
      — **UX overhaul, per explicit request:** (1) real token-by-token streaming — `run_agent` now drives `graph.stream(stream_mode=["updates","messages"])` instead of `"updates"` alone, emitting `token` events per chunk; the `message`/`done` events still carry the complete text so the frontend can swap from raw streaming text to a clean markdown render once a message finishes. (2) Assistant output and tool results render as real formatted Markdown (`marked`+`marked-highlight`+`highlight.js`+`DOMPurify`, all bundled locally, no CDN — `frontend/src/lib/markdown.ts`, `components/base/Markdown.vue`) instead of showing literal `**asterisks**`; code blocks are syntax-highlighted and open in a popup on click (`components/base/CodeModal.vue`). (3) The same rendering bug existed in the `.docx` output — new `app/tasks/docx_writer.py` turns `**bold**`/headings/lists into real Word formatting (`run.bold`, heading styles, list styles — verified by reading a generated file's actual python-docx attributes back), applied to both `draft_docx_approval_note` and Phase 4's `finalize_node`. (4) Any text document (PDF/DOCX/TXT/MD/CSV/JSON/YAML) can now be attached — `app/documents/extract.py` (reusing `pymupdf4llm`/`python-docx`, both already dependencies) + a new `read_uploaded_document` tool. (5) Auto-resizing input box, fade-in trace animations, a streaming caret. (6) Looked into MCP for docx/document tooling instead of skipping it silently: `hi/`'s own MCP usage fetches servers via `npx ...@latest`/Docker pulls at runtime, which would violate zero-egress for capability already covered natively here — didn't add one.
      — **"I want to see the model thinking" — root-caused and fixed:** a complex multi-part analysis prompt appeared to hang for 3+ minutes with zero output. Root cause: Ollama's reasoning models (gpt-oss, qwen3, ...) stream a separate `thinking` field the installed `langchain_ollama` silently drops entirely unless `ChatOllama(reasoning=True)` is set — verified live via the raw `ollama` client that `thinking` was flowing the whole time, just never surfaced. `reasoning=True` itself 400s on a model that doesn't support thinking (`qwen2.5-coder:7b`, etc.), so `app/router.py`'s `get_chat_model()` now detects this once per model (try `reasoning=True`, fall back on the specific "does not support thinking" error) rather than hardcoding which candidates think. `agent.py` now emits a `thinking` SSE event from `chunk.additional_kwargs["reasoning_content"]`; `ConsoleView.vue` renders it as a live-updating collapsible block. Live-verified: the first `thinking` event now arrives in ~2s (previously: nothing until either a tool call or a crash).
      — **Three more bugs found chasing the same investigation, all fixed:** (a) `calculate`'s unit table had no `year`/`month`/plural forms at all — `"10 years / 1 year"` silently parsed as `10*second*a**2*e**2*r**2*y**2` (each unknown letter treated as its own multiplied symbol) instead of erroring; added `day`/`week`/`month`/`year` + programmatic plural aliases, **and** a general guard that rejects any expression with leftover unrecognized symbols instead of silently mangling them — this class of bug can't recur with some other unrecognized word later. (b) the sandbox image (`python:3.12-slim`, no packages, no network ever) meant any numpy/scipy/statsmodels-based analysis code was guaranteed to fail every single retry — new `docker/sandbox/Dockerfile` (`sage-sandbox:latest`, numpy+scipy pre-installed, built once locally same as any other one-time download) fixes this at the source. (c) `run_sandboxed_code` required an *exact* stdout match even for open-ended analytical code with no single correct printed string, so correct code "failed verification" every time — `expected_output` is now optional; omitted, `app/tasks/code.py` verifies the script actually ran (exit 0, printed something) instead of string-matching a guess. (d) `fit_linear_regression` had no confidence interval at all (part of why the model reached for custom sandbox code instead of the ML tool it should have used) — added a real OLS confidence interval (`scipy.stats.t`, computed on the full dataset — standard practice for small-sample inference) plus `r2_full` alongside the existing held-out `r2_test`, and tightened the system prompt to prefer the ML tools over hand-written fitting code. **Combined effect, live-tested on the exact prompt that started this**: 3.5+ minutes / 8 tool-call rounds / eventual Ollama Cloud crash → **13 seconds / 2 rounds / correct, complete answer**, zero errors.
      — **"Still not properly streaming, only shows the full output after" — root-caused as a frontend perf bug, not a backend one.** Verified the backend genuinely streams token-by-token at both the Python level (`graph.stream(stream_mode=["updates","messages"])`, real inter-chunk time gaps) and the raw HTTP/SSE level (a plain `urllib` client against `/agent/chat/{id}/stream` showed the same real-time spread) — so the transport was never the problem. Root cause was in `ConsoleView.vue`'s `scrollToBottom()`: it was called on *every* `token`/`thinking` SSE event (dozens per second) and did `await nextTick()` then `scrollTo({behavior: "smooth"})` uncoalesced — each call queued a fresh smooth-scroll animation before the last one finished, starving the compositor of paint time for the actual text updates, so nothing visibly moved until the flood of scroll calls stopped and the whole response appeared to "jump in" at once. Fixed by coalescing to at most one scroll per animation frame (`requestAnimationFrame`, instant `"auto"` behavior while tokens are still arriving, `"smooth"` only for the one-off send/complete moments) and only auto-scrolling when already near the bottom (so it stops yanking the view back down if the user scrolled up mid-stream to reread something).
      — **Added per explicit request: LaTeX rendering.** `marked-katex-extension` + `katex` (both bundled locally via npm/Vite, no CDN — same self-hosting approach as the existing marked/highlight.js/DOMPurify pipeline) wired into `frontend/src/lib/markdown.ts`; `$...$`/`$$...$$` render as real math instead of literal LaTeX source. KaTeX's own CSS + font files import locally (`katex/dist/katex.min.css`) and bundle as static assets (verified via a production build — fonts show up as hashed `dist/assets/KaTeX_*.woff2` files, not a runtime fetch). `agent.py`'s system prompt updated to actually use this for equations/formulas. One packaging wrinkle: `marked-katex-extension` ships raw `.ts` (not `.d.ts`) as its `types` entry, which fails `vue-tsc`'s strict `noUnusedParameters` check on the *package's own* source — worked around with a local ambient declaration (`frontend/src/types/marked-katex-extension.d.ts`) redirected via a `tsconfig.app.json` `paths` mapping, rather than loosening this project's own strictness.
      — **Correction, same day: the LaTeX rendering above didn't actually work.** Reported back by you immediately after. Root cause found by testing the real model's own output rather than assuming the wiring was the problem: `gpt-oss:120b-cloud` writes LaTeX as `\[...\]`/`\(...\)` (confirmed live — asked it for the quadratic formula, got back `\[ x = ... \]`), and `marked-katex-extension` only recognizes `$...$`/`$$...$$` by design, so every equation was rendering as inert escaped text no matter what the system prompt asked for. Fixed with a delimiter-normalization pass in `markdown.ts` (`normalizeLatexDelimiters()`) that converts `\[...\]`→`$$...$$` and `\(...\)`→`$...$` before the markdown parse, skipping fenced/inline code spans so a literal `\(` in a code sample isn't touched. Verified end-to-end outside the browser (Node script feeding the model's actual `\[...\]` output through the real `marked`+`markedKatex` pipeline) — real KaTeX HTML comes out, and a `\(` inside a ```` ```python ```` block is correctly left as literal text. System prompt loosened to accept either delimiter style since both are now handled.
      — **Added per explicit request: chat session persistence.** Completed turns (on `done`/`error`) are written to `localStorage` (`sage-console-turns-v1`) and restored on mount, so a page refresh or closed tab doesn't lose the conversation — private to that browser, nothing leaves the machine, consistent with the air-gap requirement. Object-URL image/audio previews are dropped on save (they can't survive a reload) but the attachment's filename is kept so it's still visible in history. A turn still `"running"` when the page closes is restored as an interrupted error rather than a fake-finished state, since there's no live connection left to resume it. New "New chat" button (Console header, only shown once there's history) clears both the in-memory transcript and the saved copy.
      — **"Simplify the architecture, employ MCP tools for everything" — clarified with you first rather than guessed at.** You confirmed the actual goal was fewer moving parts, not literally the MCP protocol (which would add IPC/serialization overhead per tool call — likely slower, not faster — and MCP was already explicitly rejected once, in this same section above, for breaking zero-egress). Investigated the reported slowness itself first: timed the exact "Write code that prints the first 10 primes" prompt at the raw Python level (1.3–3.7s across 3 runs) and through the real HTTP/SSE endpoint the browser hits (3.24s total, first token at 2.4s) — not slow; the likely actual cause was the scroll-jank bug above, already fixed. For the real simplification, audited every API path the frontend (`ConsoleView.vue`/`KnowledgeBaseView.vue`/`DeliverablesView.vue`/`stores/models.ts`) actually calls and found `POST /route/{task_type}/invoke` (+ its stream endpoint), `POST /tasks/code`, `POST /tasks/calculate`, `POST /tasks/ml`, and `POST /tasks/transcribe` (+ streams) were **dead HTTP surface area** — the task-kind-picker UI that called them was removed back when Console became a single chat box, and every one of their capabilities is already re-exposed as an `app.agent` tool the live agent calls directly (`run_sandboxed_code`, `calculate`, the ML tools, `transcribe_audio`), so nothing was lost. Removed all five endpoint pairs from `main.py`, plus `app/orchestrator.py` entirely (the plan→act→verify skeleton `/route/invoke` was its only caller anywhere in the codebase — confirmed via grep before deleting). Verified after the cut: `uv run python -c "from app.main import app"` lists exactly the routes still in use, all five removed paths now 404 against the live server, and `/agent/chat` still runs end-to-end. **Flagged, not silently resolved:** `/tasks/document` was deliberately kept, unlike the other four — its underlying `run_document_task` is the *only* place Phase 4's hard, bounded reviewer/critic loop actually runs; the Console's free-form agent reproduces the same job today by chaining `read_uploaded_image`/`search_knowledge_base`/`draft_docx_approval_note` itself, prompted rather than mechanically enforced (this is the same gap the Phase 6 regression note above already describes fixing once via prompt/retrieval changes, not by restoring the hard loop). That means the stronger reviewer path is currently unreachable from the actual product UI. Left as-is and documented in `main.py`'s docstring rather than deleting the more robust implementation or silently leaving the duplication unexplained — worth a decision on whether to wire `draft_docx_approval_note` through the real reviewer loop instead of maintaining both.
      — **"Why is the send button disabled?" — traced to a thrown exception, not the button's own logic.** Your report ("I typed and it's not working") plus a browser console error (`Could not find the language 'plaintext'...`) pointed at `frontend/src/lib/markdown.ts`. Root cause: `highlight.js/lib/core` (the trimmed-down build used here, vs. the full bundle) only knows languages explicitly registered via `hljs.registerLanguage()` — and the code's own fallback for an unfenced/unrecognized code block was `"plaintext"`, which was never itself registered, so the fallback path threw. That throw happens inside `Markdown.vue`'s `computed()`, i.e. mid-render — Vue's render-flush error handling can abort the rest of that same batched update, which is how one bad code fence (any assistant message with an unlanguaged ` ``` ` block) could make unrelated DOM bindings in the same tick — like the Send button's `:disabled` — stop updating, even though the underlying `prompt` ref was changing correctly. Fixed at the source (`hljs.registerLanguage("plaintext", ...)`), and defensively hardened beyond that: every `hljs.highlight()` call site now goes through a `try/catch` (`safeHighlight()`) falling back to escaped plain text, and `renderMarkdown()` itself has an outer `try/catch` falling back to an escaped `<p>` of the raw message — so a future unknown-language edge case (or any other markdown-pipeline surprise) degrades to plain text instead of taking the whole Console's reactivity down again. Verified via a standalone Node script feeding both an unlanguaged fence and an unregistered-language fence (`` ```rust ``) through the real pipeline: no exception, correct highlighted/escaped output either way.
      — **Added per explicit request: multi-turn conversation memory.** Every `/agent/chat` call was previously its own stateless turn — `run_agent()` built `{"messages": [HumanMessage(prompt)]}` from scratch each time, so a follow-up like *"convert it into a word doc"* right after a long analysis had nothing to refer back to; the agent had already forgotten its own answer. Fixed with LangGraph's built-in checkpointing rather than hand-rolling message-history plumbing: `app/agent.py` gets a module-level `MemorySaver()` (shared across calls, in-process/RAM — scoped to this backend's lifetime, not persisted to disk), `create_react_agent(..., checkpointer=_checkpointer)`, and `run_agent()`/`run_agent_multilingual()` both gained a `thread_id` parameter threaded through to `graph.stream(..., config={"configurable": {"thread_id": ...}})`. The frontend generates one `conversationId` (`crypto.randomUUID()`) per chat — persisted to `localStorage` alongside the transcript so a page refresh continues the same backend thread, reset on "New chat" so a fresh conversation doesn't inherit old context — and sends it as `conversation_id` on every `/agent/chat` request (`main.py`'s new optional `conversation_id` form field, omittable for a stateless one-off call, e.g. a direct API caller). Live-tested with your exact scenario: asked the full wall-thickness corrosion-rate regression question, then — same `conversation_id`, no data repeated — *"convert it into a word doc"*; the agent correctly called `draft_docx_approval_note` with the **actual prior answer's real numbers** (intercept 12.003, slope −0.01973, the same 95% CI, R²) as the document content, not a re-fabricated or generic one. Also verified conversation isolation: the identical follow-up sent under a different `conversation_id` correctly got "I'm not sure what you'd like converted" instead of leaking the other chat's context.
      — **"Uploaded images aren't getting used as context" — a real bug, and a direct side effect of the conversation-memory feature above.** By this point in the build there are four different "something was attached this turn" tools (`read_uploaded_image`, `read_pid_drawing`, `read_uploaded_document`, `transcribe_audio`), and the model had no ground truth about which (if any) actually applied to a given message — it had to infer that purely from how the user phrased the prompt. Reproduced live: a neutral prompt ("here's a field log entry, take a look") with an image attached made the model guess `read_uploaded_document` first, get back "no document attached", and **give up** rather than trying `read_uploaded_image` — the image was never read at all. Once conversation memory exists this compounds: a later follow-up turn correctly has no `image_b64` of its own (each turn's attachment is per-request, by design), but by then the image was never read into that thread's history either, so the whole conversation permanently lost access to it. Fixed at the root in `run_agent()`: a factual `[Attached to this message: an image (read_uploaded_image or read_pid_drawing).]` (or audio/document, or omitted entirely when nothing was attached) is now prepended to the `HumanMessage` every turn, built directly from which of `image_b64`/`audio_path`/`document_text` are actually non-`None` for that call — the model is told what's there instead of guessing from phrasing. This is a one-time fix per turn, not per-tool-call, so it doesn't need updating every time a new attachment tool gets added later. Verified by replaying the exact failing scenario: the same neutral prompt now correctly calls `read_uploaded_image` immediately, and a same-conversation follow-up in a later turn (no new attachment) correctly recalls the image's content from history. Also regression-tested the P&ID cross-reference scenario above (image + document together) — same correct mismatch caught, no change in behavior for a case that already worked.
      — **"Proper support for exporting to a Word doc" — the docx writer had no table support at all and zero LaTeX rendering, confirmed by reading its actual code rather than assuming.** `app/tasks/docx_writer.py` (Phase 6's markdown-to-docx renderer) only ever handled headings/bold/italic/code/lists — a GFM pipe table fell straight into the generic-paragraph fallback, which joined every row (including the `|`/`-` separator row) into one run-on line of literal pipe characters; `\[...\]`/`\(...\)`/`$...$` LaTeX was never touched at all, shown as raw escaped source. Asked whether an MCP tool existed for this — no, and per this same section's earlier MCP rejection (breaks zero-egress, adds IPC overhead for no benefit) the fix stayed in-process: rewrote `docx_writer.py` to detect real pipe tables and render them via `doc.add_table()` (`"Light Grid Accent 1"` style, bold header row, one of python-docx's built-in styles — verified it exists in the default template before relying on it) instead of dumping them as text, and added `app/tasks/docx_math.py`: LaTeX rendered to a small transparent PNG via **matplotlib's built-in mathtext engine** (no system LaTeX/TeX install needed, ships with matplotlib itself, purely local rendering — not a native OOXML/OMML equation object, which would need either a full LaTeX→MathML→OMML converter or sourcing Microsoft's own XSLT of unclear redistribution terms for an air-gapped tool; an image reads as a properly typeset equation without that risk, just not Word-native-editable — a real trade-off, not hidden). `\[...\]`/`\(...\)` normalized to `$...$`/`$$...$$` the same way the frontend's `markdown.ts` already does. One equation mathtext can't parse falls back to showing its raw LaTeX text rather than aborting the whole document.
      — **Three real bugs found live-testing this against the actual wall-thickness scenario, not just synthetic input:** (a) mathtext doesn't support `\boxed{}` (used by the model for a highlighted result) — unwrapped to its content (brace-matched by hand, since the content commonly nests its own braces) rather than dropping the whole equation over a purely decorative wrapper. (b) mathtext doesn't support `\displaystyle`/`\textstyle`/etc. (the model reaches for `\displaystyle` often, to force full-size rendering) — these are pure style directives with no semantic effect, so stripped outright. (c) mathtext only recognizes `\leq`/`\geq`, not the `\le`/`\ge` shorthand the model used in a confidence-interval inequality (`-0.020 \le \beta_1 \le -0.019`) — aliased via regex (word-boundary-guarded so it doesn't also rewrite `\leq` itself). **A fourth bug was in this session's own fix, caught immediately by testing it before moving on:** the alias substitution first crashed with `re.PatternError: bad escape \l` — `r"\le\b"` isn't a valid *regex pattern* (Python's `re` doesn't recognize `\l` as an escape), it needed `r"\\le\b"` for a literal backslash; a second, separate mistake in the same fix (using the replacement string directly as `re.sub`'s `repl`, which has its own escape-sequence mini-language that also chokes on `\l`) was fixed by using a lambda instead. **The most consequential bug, found by testing a table cell containing `\left| x \right|`:** the table-row splitter (`_split_table_row`) naively split every line on `|` with no awareness that a LaTeX absolute-value expression contains literal pipe characters — this was the actual root cause of a truncated `"$\displaystyle"` cell fragment observed in an earlier test render (the row got sheared apart mid-equation). Fixed with a linear scan that tracks whether it's inside a `$...$`/`$$...$$`/`` `code` `` span and only treats `|` as a column separator outside one. Verified the exact failing shape directly (`_split_table_row("| Abs value | $\left| x \right| = 5$ |")` now correctly returns two cells, not four) and re-ran the full live two-turn scenario (regression question → "convert it into a word doc") multiple times afterward with no further truncation across any generated table.
      — **Open issue, not resolved:** you reported immediately after the fix above that docx tables "still don't work" — not yet re-diagnosed (every test this session, including several full live agent runs afterward, produced correctly-structured `doc.add_table()` output with no truncation, so whatever you're seeing wasn't reproduced here). At your explicit direction ("I want to move on"), this was deliberately not chased further this session — needs a concrete repro (the actual generated `.docx`, or what specifically looks wrong: no borders? wrong content? a crash?) to make progress, rather than more speculative fixes against a bug that keeps not reproducing.
      — **Added per explicit request: MS Office support beyond Word.** `python-docx`/`pptx`/`openpyxl` were named together in this project's Stack from day one (top of this file), but only the docx side had ever been built. Added the other two, matching the same "real file, not text dumped into a generic container" principle as the table/LaTeX fix above: `app/tasks/excel_writer.py` (`write_excel()`, openpyxl — bold/filled header row, real numeric cells so the sheet is usable in Excel formulas/sorting, not just readable, auto-sized columns) and `app/tasks/pptx_writer.py` (`write_pptx()`, python-pptx — a title slide plus one slide per `{heading, bullets}`, deliberately plain rather than decorative). Two new `app.agent` tools, `export_to_excel`/`export_to_powerpoint`, alongside the existing `draft_docx_approval_note`; system prompt updated to pick the Office format that actually matches the request (document → docx, data table → xlsx, presentation → pptx) instead of defaulting to docx for everything. Also extended the *reading* side to match: `app/documents/extract.py` gained `.xlsx` (openpyxl, sheets rendered as tab-separated rows so the model can read a real table) and `.pptx` (python-pptx, slide-by-slide text) support for `read_uploaded_document`/the unified upload button — and, while in that file, `.docx` reading was extended to include table content too (`doc.tables`), which it had never read before, only paragraphs. **Two real gaps caught and fixed before considering this done, both in code this session had itself left inconsistent for the *existing* docx deliverables, not just the new file types:** `GET /deliverables` filtered on `filename.endswith(".docx")` specifically, which would have made every new .xlsx/.pptx file invisible in the Deliverables view despite saving correctly; and `GET /deliverables/{filename}` hardcoded the Word Content-Type header for every download regardless of actual file type. Both fixed with a shared extension→media-type map. Verified live end to end: direct tool calls (inspected the actual generated `.xlsx` — real int/float cells, bold header — and `.pptx` — correct title/subtitle and per-slide bullets — via openpyxl/python-pptx), natural-language requests through the real agent ("export this raw data as an Excel spreadsheet" → `export_to_excel` called correctly; "make a short 2-slide PowerPoint summary..." → `export_to_powerpoint` called correctly), round-trip reading (generated an xlsx/pptx, then read them back via `extract_text()` and got correct text), and the live `/deliverables` list + download endpoints (correct `Content-Type` headers for `.xlsx`/`.pptx`/`.docx` alike, confirmed with `curl -D -` after an initial `curl -I` HEAD-request check gave misleading results — Starlette's `FileResponse` doesn't populate headers the same way for HEAD as it does for GET, a testing-methodology catch, not a product bug).
- [x] Knowledge Base view backed by Phase 3
      — `KnowledgeBaseView.vue`: search box → `GET /knowledge/search`, renders the answer, a confidence badge (Phase 3's `flag_confidence`), and the grounding chunks with source/page/section/score
- [x] Deliverables view backed by Phase 4/5 outputs
      — `DeliverablesView.vue`, backed by a new `GET /deliverables` (list) endpoint alongside the existing `GET /deliverables/{filename}` (download) from Phase 4; renders via the `VirtualDataTable` base component with a download link per row
- [x] Model Registry view rendering `models.yaml` read-only
      — went beyond read-only per your explicit ask for in-UI switching: `ModelRegistryView.vue` + a new `stores/models.ts` Pinia store render every task type's candidates (location/pulled/active badges) and switch the active one live via the already-proven `POST /models/{task_type}/active` — no restart, no code touched. Live-tested the exact call shape the UI makes (switched `vision` to local `qwen2.5vl:7b` and back)
      — `frontend/src/lib/api.ts` gained `apiPostForm` (multipart upload, for Console's document task)
      — Not verified in an actual browser (still no browser/screenshot tool in this environment, as noted in Phase 2): verified instead via a clean `vue-tsc -b` (template/prop types check out) plus live end-to-end tests of every API/SSE contract each view depends on, using the exact request shapes and confirming response shapes match the TS types
      — **Two UI requests, per explicit ask: collapse the knowledge-base tool result, and stop the app from losing its bottom-pinned input bar.** (1) `search_knowledge_base`'s tool_result (up to 6 raw retrieved excerpts, per Phase 6's earlier fix to return raw context instead of a pre-digested answer) is now a `<details>` accordion, collapsed by default with a char-count summary — every other tool's result stays as before, only this one gets long enough on a routine question to be worth collapsing. (2) The input bar not staying pinned to the bottom traced to `AppShell.vue`, not `ConsoleView.vue`: the shell's outer container was `min-h-screen` (grows with content, no cap) and `<main>` had no height/overflow constraint at all — every *other* view (Deliverables, Knowledge Base, ...) relies on exactly that to let the whole page scroll, since none of them have their own internal scroll region. `ConsoleView.vue` does (`h-screen flex-col` + an internal `overflow-y-auto` transcript), but nothing capped its ancestors, so on a long enough conversation the entire page — Console's own input bar included — could scroll away instead of only the transcript. Fixed by making the shell itself the fixed viewport (`h-screen` on the outer flex, `overflow-y-auto` on `<main>`) so page-level scrolling still works exactly as before for every view that depends on it, while `ConsoleView.vue`'s root changed from an independent `h-screen` to `h-full` (fill exactly what the now-height-capped `<main>` gives it) so its own internal transcript/input split is what actually governs Console specifically. Typecheck and a production build are clean; not verified in an actual rendered browser (same standing limitation as every other UI change this session, noted above) — worth a visual confirmation next session.

### Phase 7 — Backend: on-demand classical ML tool `[WOW]`

- [x] Parameterized tool functions, not free-form model-written code: `fit_linear_regression`, `fit_logistic_regression`, `kmeans_cluster`, `pca_reduce` (scikit-learn)
      — `backend/app/tasks/ml.py`; all four are plain parameterized functions (data + hyperparameters in, a results dict out) — no model-written code is ever executed for this tool, unlike Phase 5's coding task
- [x] Enforce train/test split inside the tool itself; return R²/accuracy + sanity bounds check before orchestrator reports success
      — `fit_linear_regression`/`fit_logistic_regression` split internally (`sklearn.model_selection.train_test_split`) and report train **and** held-out test R²/accuracy (plus a majority-class baseline for logistic) so a caller can't mistake a memorized training score for a real one. `kmeans_cluster`/`pca_reduce` are unsupervised — no train/test split applies — so they report the closest equivalent quality signal instead (silhouette score; cumulative explained variance). Every function returns `sanity_ok`/`sanity_note` (score outside its theoretically-possible range, e.g. accuracy outside [0,1]) before the caller trusts the result
      — **Added later, found missing while live-testing a real inference request:** `fit_linear_regression` originally reported only point-estimate coefficients and test R², no uncertainty on the slope — part of why the agent reached for hand-written sandbox code instead of this tool on a "give me a confidence interval" request. Added a real OLS confidence interval per coefficient (`scipy.stats.t`, standard error from the residual covariance matrix, computed on the **full** dataset rather than the train split — the standard approach for small-sample inference, where holding out data just to fit would waste scarce degrees of freedom), a new `confidence_level` parameter (default 0.95), and `r2_full` alongside the existing `r2_test`. System prompt updated to prefer this tool over hand-written fitting code in the sandbox.
- [x] Orchestrator picks tool + args from natural-language request; model interprets result, doesn't compute it
      — reuses Phase 6's agent (`app/agent.py`) rather than a separate mechanism: the 4 ML functions are added as bound tools next to the Phase 3/4/5 ones, so the same `create_react_agent` loop already driving the Console picks the right one from plain-language requests. Live-tested: *"I have this data: X=..., y=... Fit a model and tell me the slope and how good the fit is"* → correctly called `fit_linear_regression` (never estimated the slope itself) and reported the real fitted coefficients/R² back in its own words
- [x] `POST /tasks/ml`
      — `backend/app/main.py`; single endpoint, `{tool, args}` dispatches via `app/tasks/ml.py`'s `run_ml_tool()` — synchronous like `/tasks/calculate` (deterministic scikit-learn, no LLM in this endpoint's own loop, nothing to stream)
- [x] have guardrails in place so that random queries wont execute
      — every function validates before touching scikit-learn: shape mismatches (`X`/`y` length), too few samples for a meaningful split (`MIN_SAMPLES = 10`), non-finite (NaN/Inf) values, and out-of-range hyperparameters (`n_clusters`/`n_components` larger than the data supports) all raise `MLToolError` → HTTP 400, never a fit. Live-tested all 5 rejection paths (too-few-samples, mismatched lengths, NaN input, oversized `n_clusters`, oversized `n_components`) plus an unknown-tool-name request — all correctly refused with a clear reason. Also tested guardrail behavior *through* the agent: a 3-sample fit request was refused by the tool (not silently fabricated), and an unrelated question ("what day comes after Tuesday?") correctly triggered zero ML tool calls
      — **Bug reported live: `fit_linear_regression` crashed with an empty error on the exact single-variable data used throughout this session's regression examples.** Root cause: the tool's own type hint was `X: list[list[float]]`, but the natural way to pass "one x per data point" (e.g. `months = [0, 3, 6, ...]`) is a flat list — and the agent's tool-calling schema is generated from that Python type hint, so Pydantic rejected the flat `X` *before the function body ever ran*, meaning `ml.py`'s own `MLToolError` handling never got a chance to produce a useful message. Fixed at both layers: `app/tasks/ml.py` gained `_to_feature_matrix()` (reshapes a flat 1D list to an (n, 1) column vector automatically — the natural shape for single-variable regression — instead of rejecting it) used by all four functions; `app/agent.py`'s four ML tool signatures widened to `X: list[float] | list[list[float]]` so the schema itself accepts the common case, with docstrings explaining both shapes. Verified by replaying the exact failing call (`X=[0,3,...,33]`, `y=[12,11.94,...,11.35]`) directly against the tool and, separately, through a live `/agent/chat` request reproducing the same model behavior end to end — both now return a correct fit instead of erroring.

### Phase 8 — Backend: regional language + TTS layer `[WOW]`

- [x] Add local `faster-whisper` speech-to-text for uploaded audio and microphone input, including language detection, timestamps, and transcript streaming into the Console
      — `backend/app/speech/stt.py` (model size `small`, CPU/int8). `POST /tasks/transcribe` + SSE stream for standalone use; also wired as an agent tool (`transcribe_audio`, `app/agent.py`) so attaching audio to a Console message works through the same single chat interface as everything else — the agent transcribes it first, then acts on what was actually asked. Frontend: `ConsoleView.vue` gained an audio-file attach button **and** in-browser mic recording (`MediaRecorder`), both feeding the same upload path. Live-tested: English and Hindi synthesized speech (macOS `say`, since no real recordings were available) both transcribed correctly with correct language detection; the full attach-audio → transcribe → search-knowledge-base → cited-answer chain confirmed end to end through `/agent/chat`
      — **UX changed later, per explicit request: "transcript and not record."** `POST /tasks/transcribe` (deleted in the architecture-simplification pass, Phase 6) is back as a leaner `POST /transcribe` — synchronous, no task_id/SSE (a single short clip has nothing worth streaming segment-by-segment), returns `{text, language, language_probability}` directly. The mic button and an uploaded audio file (via the new unified upload button below) no longer become a chat *attachment* the agent transcribes later — `transcribeAndInsert()` in `ConsoleView.vue` calls `/transcribe` immediately and drops the resulting text straight into the prompt box (appended after anything already typed), so the user sees and can edit the transcript before it's ever sent, same as typing. This means the agent no longer sees "audio was attached" for anything coming through the Console UI, so it never calls the `transcribe_audio` tool from there anymore — that tool and `/agent/chat`'s `audio` field are left in place (still exercised by `/transcribe`'s underlying function, still usable by a direct API caller), just no longer reachable from this UI. Live-tested: a synthesized English question (macOS `say`) recorded through the mic button correctly landed as editable text in the box before sending.
      — **Also per explicit request: one unified upload button.** The three separate attach buttons (image/document/audio-file) are now one (`UPLOAD_ACCEPT` covers all three), dispatched by MIME type in `onUploadChange()` — an image or document attaches as before; an audio file goes through the same `transcribeAndInsert()` path as the mic button rather than attaching.
- [x] ~~Add Google Translate...~~ — **dropped**, not built. Google Translate has no offline mode at all (confirmed: it's a pure cloud API), which the very next bullet already asks to verify against — flagged to you before writing any code; you chose IndicTrans2 only. See README's Phase 8 section for the full reasoning captured at decision time.
- [x] Verify the translation path against SAGE's air-gapped requirement: Google Translate must not make runtime network calls; use an offline-compatible/local translation model or pre-downloaded service before marking this complete
      — verified by not using Google Translate at all (see above) — IndicTrans2 is the local model used instead, `location: local` in `models.yaml` is accurate for it (inference is 100% on-device; only the one-time weight download needs authentication, same category as `ollama pull`/`ollama signin`)
- [x] Language ID (fastText `lid.176`) on incoming query
      — `backend/app/language/detect.py` + `scripts/download_lid_model.py` (one-time public download, no auth, ~131MB). Live-tested on real text in all 5 target languages plus English — all correctly identified with >93% confidence. Worked around a real numpy 2.x incompatibility in the installed `fasttext-wheel` package's `predict()` wrapper (calls the underlying `model.f.predict()` binding directly instead)
      — **Swapped `fasttext-wheel` for `fast-langdetect`, per explicit request.** Same underlying fastText lid.176 model, different Python packaging — but a real improvement, not just a lateral swap: `fast-langdetect`'s `model="lite"` mode ships a compressed (~1MB) copy of lid.176 *inside the pip package itself*, so there's no separate download step at all, eliminating `scripts/download_lid_model.py` and the `backend/data/models/lid.176.bin` file entirely (both removed, along with the now-orphaned `backend/data/models/` `.gitignore` entry). Verified accuracy wasn't traded away for that convenience: re-ran the exact same 5-language-plus-English test set this phase originally used — 6/6 correct, confidences 0.947–1.000, comparable to or better than the old setup's >93%. Also sidesteps the numpy 2.x workaround entirely (`fast-langdetect` has its own maintained wrapper, not the abandoned one `fasttext-wheel` shipped). One real hazard hit and fixed during the swap: `uv remove fasttext-wheel` silently broke `fasttext-predict` (fast-langdetect's actual runtime dependency) — both packages install their Python files under the same `fasttext` import path, and uv's uninstall of one deleted files the other needed, leaving `import fasttext` failing with a bare `ModuleNotFoundError` even though `fasttext-predict` was still listed as installed. Caught by actually re-running the test suite immediately after the swap rather than assuming it worked; fixed with `uv sync --reinstall-package fasttext-predict`.
- [x] IndicTrans2 for Tamil/Hindi/Telugu/Kannada ↔ English pivot translation, added to `models.yaml` same as core models
      — `backend/config/models.yaml`: new `translation_en_indic`/`translation_indic_en` task types (`engine: transformers` — a new field on `ModelCandidate`, defaulting to `"ollama"` for every existing entry, since these two aren't Ollama-servable). `backend/app/language/translate.py` loads them via HuggingFace `transformers` directly. **Also covers Malayalam** (named in the dropped Google Translate bullet but not in this one) since IndicTrans2 supports it natively — one unified language set instead of two mismatched lists.
      — **NOT LIVE-VERIFIED beyond the point of an auth wall**: every official IndicTrans2 repo on HuggingFace is gated (free HF account + accepting terms on the model page + `HF_TOKEN`) — something only you can do, not something this session could complete unattended. Flagged to you; you chose "build it now, verify later." What *was* verified live: the plumbing works right up to the actual weight download — a real transformers-version incompatibility in `IndicTransToolkit` 1.1.1 (stale import path against transformers 5.x, which Phase 3's reranker already requires — can't pin transformers down) was found and fixed with a small, documented compatibility shim; language detection on a Hindi prompt through `/agent/chat` correctly triggers the translation path and fails with the exact expected `401 gated repo` error, not a crash. **To finish verifying:** create a free HF account, accept the terms at `huggingface.co/ai4bharat/indictrans2-en-indic-dist-200M` and the `indic-en` counterpart, set `HF_TOKEN`, then re-run the same Hindi prompt through the Console.
      — **Progress, not yet fully unblocked: you provided an `HF_TOKEN` and asked it be used.** Restarted the backend with it set and re-ran the exact translation path live — the error changed from `401` (not authenticated at all) to `403 ... you are not in the authorized list` on both gated repos. That's real, meaningful progress: **the token itself is valid and being sent correctly** — what's still missing is the actual "Agree and access repository" click-through on the two model pages (`huggingface.co/ai4bharat/indictrans2-en-indic-dist-200M` and the `indic-en` counterpart), which only the account holder can do in a browser — a token alone doesn't grant access to a gated repo, it just authenticates *as* an account, and that account hasn't accepted the terms yet. The token itself was used only to launch the backend process (`HF_TOKEN=... uv run uvicorn ...`) and was never written into any file in the repo, checked in, or echoed back in full — it only ever existed as a process environment variable for this session. **To finish:** visit both model pages while logged into the account that owns this token, click through the terms on each, then restart the backend with `HF_TOKEN` set (as already documented above) and retry.
      — **Update: the terms got accepted (reported as "No module named 'transformers.onnx'") — gated access is fully resolved, but four more, deeper compatibility breaks surfaced one at a time behind it, and one final one remains genuinely unfixed.** Confirmed the gate itself cleared: the `403 not authorized` from the entry above is gone, both ~200M-parameter models actually download now. What follows is real weight-loading/inference archaeology, each issue found by running the real thing and reading the next traceback, not guessed — `app/language/translate.py` now documents all of it inline:
        - **Shim 1 (pre-existing, unchanged):** `IndicTransToolkit`'s own import path issue, already fixed in an earlier session.
        - **Shim 2 — `ModuleNotFoundError: No module named 'transformers.onnx'`** (your exact report): the model repo's own remote `configuration_indictrans.py` imports `OnnxConfig`/`OnnxSeq2SeqConfigWithPast` from a submodule transformers 5.x removed entirely (ONNX export moved to the separate `optimum` package). Confirmed by reading the cached remote file directly: those classes are used *only* to define an `IndicTransOnnxConfig` this app never instantiates. Fixed with a minimal stub module registered in `sys.modules`.
        - **Shim 3 — `AttributeError: IndicTransTokenizer has no attribute _special_tokens_map`:** the remote tokenizer sets `self.unk_token = ...` etc. *before* calling `super().__init__()`, which is what transformers 5.x now requires to have run first (it intercepts special-token attribute writes and routes them through a dict that doesn't exist yet). Fixed by lazily creating that dict on first write instead of requiring `__init__` to have created it already.
        - **Shim 4a — `TypeError: ...tie_weights() got an unexpected keyword argument 'recompute_mapping'`:** transformers 5.x's own `init_weights()`/`from_pretrained()` now always call `tie_weights(recompute_mapping=...)`, but the remote model overrides `tie_weights()` with the old zero-argument signature. Fixed generally (not just for this one class) via `PreTrainedModel.__init_subclass__` — a standard Python hook that fires for every subclass, including ones `exec`'d from a dynamically downloaded remote file — wrapping any subclass's `tie_weights` that can't accept the new kwargs, checked by real signature inspection so it's a no-op for any normal, up-to-date model class.
        - **Shim 4b — `AttributeError: ...object has no attribute '_tie_or_clone_weights'`:** the now-reached remote `tie_weights()` calls a `PreTrainedModel` utility method that's been fully removed from transformers 5.x. Restored as the well-known historical implementation (share the weight tensor, or clone under torchscript; pad bias; fix `out_features`) — simple, self-contained, doesn't depend on any other removed internals.
        - **Shim 4c — `TypeError: 'EncoderDecoderCache' object is not subscriptable`, then `AttributeError`/`RuntimeError` chasing it:** the remote model's hand-written attention forward() does old tuple-style KV-cache indexing (`past_key_values[0][0].shape[2]`, `torch.cat([past_key_value[0], key_states], dim=2)`), but transformers 5.x replaced the plain-tuple cache with `Cache`/`EncoderDecoderCache` objects that support iteration, not integer indexing. Added `__getitem__` (delegating to the existing `__iter__`) plus a zero-length placeholder for an as-yet-untouched layer's `None` keys/values (old cache semantics treated "nothing cached yet" as an empty tensor, not `None`). **First tried the much simpler `use_cache=False`** to sidestep this whole codepath — it avoided the crash but silently produced *empty output for every input*, no error at all: caching turned out to be load-bearing for this model's generation quality, not just a performance nicety, and this was worth catching before trusting it. Reverted that in favor of the real fix.
        - **Where it's stuck now, unresolved:** even with all of the above, `.generate()` still fails inside the remote model's custom self-attention forward() at `torch.cat([past_key_value[0], key_states], dim=2)` — a shape mismatch, because the zero-length placeholder from Shim 4c isn't shaped like a real (batch, heads, 0, head_dim) tensor (those dimensions aren't knowable from outside the attention layer where the cache is queried). This is a materially different *kind* of problem than shims 1–4b: not a missing import/method/kwarg, but the remote model's own low-level attention math not matching the new Cache object model structurally. Deliberately stopped here rather than keep guessing at tensor shapes with no way to verify correctness short of a fluent Hindi/Tamil/etc. speaker checking real output — especially given the `use_cache=False` episode already proved this specific failure mode (silently wrong/empty output, no crash) is real and easy to reintroduce by patching the wrong thing. **Net result:** everything up to and including starting real inference now works and is verified; actual translation output is still not achievable without either properly patching this model's attention implementation (bigger, riskier) or isolating this one path against an older transformers install. All four shims are kept regardless — genuine, verified progress, not wasted effort, and harmless to every other feature in this app (defensively guarded, only activate for the specific broken cases, confirmed live that an ordinary English request still works correctly after all of this).
- [x] Core pipeline stays English-internal — no changes to Phases 1–5
      — `app/language/pipeline.py`'s `run_agent_multilingual()` wraps `app.agent.run_agent` from the outside (translate prompt → call the unchanged agent → translate the answer back); zero lines touched in Phases 1–5's own code. English prompts (the common case, and everything tested through Phase 7) skip the translation path entirely — confirmed via a live regression test that the exact same English question used throughout this build still produces the identical grounded, cited answer through the new wrapper
      — **"Add regional language support for output as well" — the backend already did this; the frontend was burying it.** `run_agent_multilingual()` always did translate the final answer back (`translated_response` event) — but `ConsoleView.vue` rendered it as a small secondary "Translated back: ..." footnote *underneath* the already-fully-rendered English answer, which had already streamed in live and stayed on screen unchanged. So the actual visible response for a non-English question was English, with the real translated answer demoted to an afterthought. Fixed: `translated_response` now replaces the primary answer segment's content directly (the translated text becomes the real rendered Markdown answer), with the English draft kept behind a "Show original (English)" toggle instead of shown by default. Also added a `translating` segment/indicator: `translate_from_english()` runs synchronously with no event of its own once the English draft finishes, which was a silent multi-second gap between the English text appearing and it flipping to translated — now shown honestly as "Translating…" instead of looking like nothing is happening (same "make the model's real work visible instead of a blank wait" principle as the Phase 6 thinking-visibility fix). If translation itself fails, `pipeline.py` falls back to the English content via `done` — the frontend now clears the stale "Translating…" indicator in that case too rather than leaving it stuck.
      — **Follow-up, per explicit request: "have the output of the ai agent also be translatable into the regional languages" — decoupled output language from input language entirely.** Until now the *only* way to get a translated answer was to ask the question in that language; an English question always got an English answer back, with no way to request "answer in Hindi" independently of what you typed. `run_agent_multilingual()` gained an `output_language` parameter that, when set, wins outright over the old mirror-the-input-language default — including `"en"`, which now means "answer in English regardless of input language" (a real, explicit override, not merely "unset"). `main.py`'s `/agent/chat` gained a matching optional `output_language` form field. Verified the target-language selection logic directly against all the combinations that matter (English in + Hindi requested → translates; Hindi in + no request → mirrors back to Hindi as before; Hindi in + English explicitly requested → suppresses the mirror and stays English; etc.) — all correct. Live-tested the actual new capability (English prompt + `output_language=hi`) against the running backend: correctly took the new code path and attempted `translate_from_english()` — which hit the same already-documented, pre-existing gated-HuggingFace-repo wall as the rest of this phase (not a new problem) and degraded exactly as designed: a `language_warning` plus the English content still delivered via `done`, not a crash. Frontend: a small "Answer in [Auto/English/Hindi/Tamil/Telugu/Kannada/Malayalam]" selector in the Console header, sent as `output_language` on every request when set to something other than Auto. Also had to fix the "Translating…" indicator's trigger condition (added in the entry above) to match: it was gated only on a `language_detected` segment existing, which only fires for non-English *input* — under the new decoupled logic that missed the English-in/translated-out case entirely, and would have also incorrectly kept showing "Translating…" for a non-English input with English explicitly requested as output (where the backend correctly does *not* translate). Rewritten to mirror `pipeline.py`'s own target-language logic exactly, keyed off which output language was actually requested for that specific turn (captured at send time on the `Turn` object, not read live off the dropdown later, since the dropdown can change while an earlier turn is still streaming). Not live-verified end-to-end with a real translated answer (same IndicTrans2 auth-gate limitation as the rest of Phase 8) — the logic and its failure-mode handling are proven correct, the final "does it read right in Hindi" check needs the same `HF_TOKEN` step already pending from earlier in this phase.
      — **Redesigned again, per explicit request: toggle buttons instead of a dropdown, and "I want everything to change as soon as something is clicked."** The dropdown only ever affected the *next* message you sent — every answer already on screen stayed in whatever language it was generated in, which isn't what "instant" means. Two changes: (1) `ConsoleView.vue`'s `<select>` replaced with a row of toggle buttons (Auto/EN/HI/TA/TE/KN/ML — Auto kept, not dropped, since it's what preserves the existing "ask in Hindi, get Hindi back" default for anyone who never touches the control). (2) Clicking a toggle now **immediately re-translates every answer already visible in the conversation**, not just future ones — new `POST /translate` endpoint (`main.py` + `app/language/translate.py`'s already-existing `translate_from_english()`, just exposed directly rather than only reachable through the full agent pipeline) takes already-generated text and a target language, synchronous (short text, fast enough not to need the task/SSE machinery). Every text segment always keeps a stable English source (`originalEnglish`, reused from the earlier "Show original" feature rather than adding a parallel field) so re-translating Hindi→Tamil correctly re-derives from the English original instead of chaining translations through a language IndicTrans2 doesn't directly support translating between (it's only en↔indic, never indic↔indic). "Auto" is the one toggle without a single fixed target — each turn can have its own natural language if the conversation itself switched languages mid-chat, so clicking it computes each turn's own target from whether *that* turn had a `language_detected` segment, rather than assuming English for everything. All translate calls for a click fire concurrently (`Promise.all`) rather than one-by-one; a `retranslating` flag disables the toggle row and shows a pulse while in flight, and a failure (still likely right now — same pending IndicTrans2 auth gate as everywhere else in this phase) surfaces as a small inline error instead of silently doing nothing. Typecheck and production build both clean; the actual "click HI and watch it translate" behavior can't be fully verified until the auth gate above is cleared, same standing limitation as the rest of Phase 8's output-language work.
- [ ] Indic Parler-TTS (or equivalent) for spoken output, one entry per language in the registry
      — **deliberately deferred**, not silently dropped: this phase's own cut-order note says "if short on time, drop TTS first," and given how much of this phase's other scope turned out to carry real integration risk (two separate third-party version-incompatibility bugs found and fixed above), this is the point to stop rather than take on a third heavy, unfamiliar model dependency in the same session. Revisit after IndicTrans2 is confirmed working.

### Phase 9 — Multimodal + P&ID symbol detection `[WOW]`

- [x] Apply Phase 4's vision pipeline to a second artifact type (handwritten note / drawing)
      — deliberately not the same kind of artifact as Phase 4's scanned inspection *form* (`inspection-report.png`): `data/sample_inputs/handwritten-note.png` (generated via `generate_phase9_samples.py`, Pillow + macOS's built-in Bradley Hand font — no new download, air-gap-safe) is a free-form handwritten field-log entry, not a structured form. No new code needed for the actual pipeline — Phase 6 already generalized image reading into `read_uploaded_image`, callable on any attached image via the same single Console chat — this task was about proving it on a genuinely different artifact type and a genuinely different kind of gap than Phase 4's. Designed the note's content as a real, catchable SOP violation rather than a trivial OCR test: a technician logs tagging a valve without a lock (permitted by SOP-LOTO-400 Step 3.2 *only* when the device truly can't be locked, *and* only with the Step 2.4 zero-energy verification also done) but only documents an informal pressure-gauge glance, not the actual required control-actuation test. Live-tested end to end: the agent read the handwritten note, searched the LOTO SOP, and correctly caught the missing Step 2.4 verification, citing the exact clause. **Also caught, and worth being honest about:** the same answer separately cited "Section 5" and invented a "LOTO Exception Form (Form LO-EX-101)" requiring supervisor approval for tag-only use — checked against the actual retrieved SOP text, Section 5 is "Lock Removal by Others" (a different scenario, lock removal when an employee is unavailable) and no such form exists anywhere in the corpus. The core finding (missing zero-energy verification) is accurate and correctly cited; this second claim is a fabricated citation the model added on its own, a general citation-fidelity gap this phase surfaced rather than something Phase 9 itself introduced or that got fixed here — flagged rather than quietly left out of this writeup.
- [x] Small, well-tested P&ID symbol set (valves, instruments, line types) cross-referenced against a sample spec sheet
      — no trained computer-vision model (unrealistic for this environment/timeframe, and there's no P&ID-labeled training data available) — used the same tool the rest of this project uses for grounded extraction: the vision model, but constrained to a small, explicit, *documented* legend instead of free-form guessing, the same "ground it, don't trust it" principle as everything else in `app/agent.py`. New tool `read_pid_drawing` (`app/agent.py`): a fixed prompt spelling out exactly five symbol categories — bowtie alone = gate valve, bowtie + actuator box = control valve, bowtie + relief flag = pressure safety valve, open circle with letters = field instrument bubble, thick vs. thin line = process pipe vs. instrument signal line — and instructing the model to report what's *actually drawn*, not what a tag name implies (a real risk: an unwary read could see "FV-2202" and assume "flow valve = control valve" without looking at the symbol at all). Sample artifacts (`generate_phase9_samples.py`): a small synthetic P&ID (`pid-drawing.png`, three tagged symbols on one process line, drawn with Pillow — no CAD tool needed, these are just polygons/circles/lines) plus a companion equipment spec sheet (`pid-line-6hc2201-spec.md`) with **one symbol/spec mismatch deliberately planted**: the drawing shows FV-2202 as a plain manual gate valve, but the spec requires a pneumatic-actuated, fail-closed control valve (tied to a HAZOP action item, so the mismatch has real consequence, not just a label difference). "Well-tested" taken literally: live-tested `read_pid_drawing` in isolation first (all three symbols identified correctly, including correctly reporting FV-2202 as "bowtie only, no actuator box attached" rather than inferring a type from its tag) before testing the full cross-reference. Then live-tested the complete flow — both the P&ID image and the spec sheet attached to one Console message, asked to cross-reference — and the agent correctly matched PSV-2201 and PT-2203 (spec met) and correctly flagged FV-2202 as the one mismatch, with the right consequence (can't provide the required fail-closed/remote-modulated action) and the right next step (escalate to Process Engineering). It also, unprompted, used the brand-new `export_to_excel` tool to produce a real comparison spreadsheet alongside its chat answer — the two most recent features composing correctly together without anything special wired for that.

### Phase 10 — Cross-document contradiction detection `[WOW]`

- [ ] Given a new finding, retrieve related SOP/correspondence via Phase 3, check numeric/policy conflicts
- [ ] Surface flags in deliverable + Console trace panel
- [ ] Only start after Phases 1–6 are solid — hardest wow feature here

### Phase 11 — Load-aware routing `[WOW]`

- [ ] Router polls GPU memory/utilization, falls back gracefully under load
- [ ] Show load/fallback status on Model Registry view

### Phase 12 — Zero-egress proof `[CORE]`

- [ ] Wrap `nethogs`/`iptables` into a monitor process, expose via `GET /system/network` or SSE
- [ ] Network Monitor view: large "0 external calls" live indicator + connection log
- [ ] Integration test: run every task type from Phases 3–11 with monitor active, confirm zero calls throughout

### Phase 13 — Deliverables polish + high-volume data table `[CORE]`

- [ ] Virtualized history table, smooth at 1000+ rows
- [ ] Bulk actions: multi-select, batch re-run, batch export
- [ ] Batch upload / queue mode with live per-item progress
- [ ] Visual consistency pass across all screens

### Phase 14 — Integration, rehearsal, buffer `[CORE]`

- [ ] Run the acceptance checklist above end to end, honestly
- [ ] Rehearse the network-unplug demo moment through the actual UI
- [ ] Remaining time is buffer, not new-feature time

## Cut order if behind schedule

11 (load-aware routing) → 10 (contradiction detection) → 9, keep basic multimodal, drop P&ID → 8, keep translation, drop TTS → stop cutting. Phase 7 (ML tool) stays — it's cheap and low-risk relative to payoff. Phases 0–6, 12–14 are the acceptance-checklist baseline; never cut those for a wow feature.
